/**
 * @arkyc/playground
 *
 * Example integration: create a session via the SDK, launch the widget,
 * display the result, and show a received webhook.
 * Stub created in Phase 0 — the real example app is built in Phase 14.
 */
export const PACKAGE_NAME = '@arkyc/playground'

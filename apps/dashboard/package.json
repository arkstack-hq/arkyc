{
  "name": "@arkyc/dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "Arkyc dashboard management UI",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@arkyc/sdk": "workspace:*",
    "@arkyc/types": "workspace:*"
  }
}

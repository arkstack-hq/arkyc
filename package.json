{
  "name": "arkyc",
  "version": "1.2.9",
  "private": true,
  "description": "Open-source identity verification platform built on Arkstack",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsdown && pnpm --filter @arkyc/api run build",
    "play": "pnpm --filter @arkyc/playground dev",
    "build:libs": "tsdown",
    "dev": "pnpm --filter @arkyc/api run dev",
    "dev:libs": "tsdown --watch",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test --pass-with-no-tests",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.0.0",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.4",
    "prettier-plugin-brace-style": "^0.10.1",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1",
    "vitest": "^4.1.9"
  }
}
